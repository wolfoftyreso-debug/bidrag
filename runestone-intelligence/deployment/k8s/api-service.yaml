apiVersion: v1
kind: Service
metadata:
  name: runestone-api
  namespace: runestone
  labels:
    app.kubernetes.io/name: runestone-api
    app.kubernetes.io/part-of: runestone-intelligence
spec:
  selector:
    app.kubernetes.io/name: runestone-api
  ports:
    - name: http
      port: 80
      targetPort: http

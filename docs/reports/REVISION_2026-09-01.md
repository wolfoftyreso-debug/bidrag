# Revision 2026-09-01 — "Ifrågasätt allt, testa allt"

Fullständig genomgång på begäran. Metod: (1) varje siffra och påstående i
styrdokumenten mot koden, (2) varje körbar kontroll körd skarpt — inklusive de
webbläsarkontroller CI aldrig kör, belastningstest och en ny systematisk
seedkontroll, (3) adversariella probes mot körande API, (4) rotorsak på varje
avvikelse i stället för omkörning. Allt nedan har bevis; ingenting är antaget.

Föregående revision: `REVISION_2026-08-29.md`. Samma dag (före denna):
produktionsartefakten genomkörd lokalt, RLS-fällan och körbokens fel
migrationssiffra (14→15) — se `docs/DEPLOY-AGENT.md`.

## Sammanfattning

| Klass | Fynd | Rättade nu | Kvar |
|---|---|---|---|
| Kod/kontroll-defekter | 5 (F2, F8, F13, F14, F15) | 5 | 0 |
| Dokumentdrift | 4 dokumentgrupper (F1, F3, F4, F13b) | 4 | 0 |
| Kureringsläge i seeden | 3 klasser (F5, F6, F7) | 0 — kräver seed + 10 översättningar per post | 17 + 2 + ~44 poster |
| Odokumenterad premiss | 1 (F9) | — produktägarbeslut | 1 |
| Gröna batterier | 9 (F10–F12) | — | — |

Produkten gick igenom allt som gick att köra. Det som brast var återigen
**kontroll- och dokumentationslagret**: tre räknare/dokument som sa "friskt"
om något som inte var det. Det är samma mönster som 2026-08-29, och det
förtjänar att sägas rakt: systemets självbeskrivning driver isär från systemet
snabbare än den underhålls. Varje generator/vakt som byggts i dag är ett svar
på det.

## Fynd

### F1 — Testantalen i CLAUDE.md och README var fel (RÄTTAT)

CLAUDE.md sa 90 core-tester och 213 api-tester; README sa 90 och 198.
Uppmätt: **100** (`vitest run` i packages/core) och **225** (räknade
`it(`/`test(` i apps/api/test). Tre olika felaktiga siffror i två
styrdokument för samma sak.

### F2 — Den GENERERADE handboken hade ett hårdkodat "72 stöd" (RÄTTAT)

`docs/MANUAL.md` är en byggprodukt med reaktivitetsvakt i verify — och sa
ändå "72 stöd" på två rader. Orsak: `tools/genmanual.mjs` hade talet
hårdkodat i två instruktionssträngar (`POST /v1/projects/:id/matches`,
`db:seed`), skrivna före seeden laddas. Vakten fångar *ocommittad*
regenerering, inte fel i generatorn själv. Rättat med en platshållare som
substitueras efter seedladdning; handboken säger nu 85.

### F3 — Levande styrdokument med föråldrade tal (RÄTTAT)

| Dokument | Sa | Är |
|---|---|---|
| CONTENT_ENGINE.md | 77 sidor; 72 stöd, 35 finansiärer, 36 källor | 170; 85, 36, 37 |
| ACTIVATION.md | "alla 72 stöd" | 85 |
| APPLICATION-INTELLIGENCE.md | 71/72 stöd har schema | 71/85 |
| SEO_CURRENT_STATE.md | 72 kurerade stöd | 85 |
| AUTHORITY_LOAD_MAP.md | alla 72 stöd | 85 |
| SEO_FUNDING_GRAPH.md | 72 stöd, 35 finansiärer, 100 kriterier | 85, 36, **348** |

Daterade ögonblicksbilder (SEO_REVISION_01, IMPLEMENTATION_LOG,
CURRENT_STATE_AUDIT, SEO_AUDIENCE_ATLAS, SEO_DATA_ACCESS_MATRIX) lämnades
orörda — de var sanna när de skrevs och säger vilket datum. GATE0_REPORT:s
"77 fynd" är ett fyndantal, inte ett sidantal (falskt positivt i sökningen).

### F4 — Den borttagna 39 kr-modellen levde kvar i tre levande dokument (RÄTTAT)

- `ENTITY_FOOTPRINT.md`: **60-sekundersbeskrivningen** — den kanoniska texten
  avsedd för LinkedIn och register — sa "analys 39 kr".
- `LANGUAGE_GUIDE.md`: CTA-exemplet lärde ut "Lås upp din bidragsanalys — 39
  kr", exakt den formulering `tools/semanticguard.mjs` förbjuder.
- `DEPLOYMENT.md`: "produktionstest med en riktig 39 kr-betalning".

Rotorsak: semanticguard skannar live-ytorna, inte docs. En blankettregel för
docs vore fel (PRODUCT_DOCTRINE §12 beskriver medvetet den borttagna modellen).
Kvar som risk: nästa modelländring kan drifta på samma sätt.

### F5 — 17 överlastade faktavägar, inte en (KURERINGSLÄGE, verktyg byggt)

PERFECTION_BACKLOG M16 beskrev ett fall (`project.sector = culture` med
"kulturprojekt" mot "filmprojekt"). Den nya kontrollen `tools/seedintegrity.mjs`
(klass A) hittar **17** — samma (faktaväg, op, expected) med olika
intagsfrågor. De farligaste, där frågorna säger *olika saker*:

| Faktaväg | Frågorna |
|---|---|
| `person.ageUnder29` | "18 och 28 år" / "19 och 29 år" |
| `organisation.youthMembersShareOver60` | "under 26 år" / "mellan 6 och 25 år" |
| `person.hasLastingDisability` | "du eller någon i hushållet" / "du eller ditt barn" |
| `person.lowHouseholdIncome` | "hushållets sammanlagda inkomst" / "din inkomst" / "låga i förhållande till boendekostnaden" |
| `person.professionalArtist` | 5 varianter, från "yrkesverksam konstnär" till "är verksamheten professionell" |

Mekaniken: ett ja-svar sätter ett faktum som andra stöd läser med annan
innebörd. En 29-åring som svarar ja på "19–29" passerar bostadsbidrag unga
(18–28). Det är en matchningskorrekthetsklass i produkten, inte bara i
SEO-lagret. Åtgärden per post är seedändring + i18n-poster i tio filer, alltså
ett kureringspass, inte en commit. `npm run seed:integrity` kör i verify som
rapport; klass C/G (motsägelser, okända aktörer) fäller.

### F6 — Två stöd utan egna villkor (PRODUKTBESLUT)

`fk-tandvardsbidrag` och `region-hogkostnadsskydd-vard` har bara hårda
kriterier (sökandetyp + land) och matchar varje privatperson i Sverige. Sant i
sak — de är universella — men de visas som en *bedömning*. M18.

### F7 — Ansökningslänken är generisk för 80 av 85 stöd (KURERINGSLÄGE)

`applicationUrl === sourceUrl` för 80 stöd; bara **41 distinkta** adresser för
85 stöd. 16 Försäkringskassan-stöd pekar alla på `forsakringskassan.se/privatperson`,
6 CSN-stöd på `csn.se/`, 5 Arbetsförmedlingen-stöd på `arbetsformedlingen.se/`.
`seo-dataqa` säger "alla har ansökningslänk" — sant, men tomt: "ansök själv
direkt hos myndigheten" lämnar användaren på en landningssida där hen måste
kunna stödets *namn*, vilket är exakt det doktrinen lovar att hen inte ska
behöva. Det försvagar också entitetsgrafen (`sameAs` på 16 stöd pekar på
samma URL). `amountSourceUrl` finns på 7 av 85. M17.

### F8 — `verify:ui` föll första gången: regelpublicering tog 16,2 s (FLAKE, ORSAK OKÄND)

uicheck2 väntar 10 s på "Ny regelversion publicerad". API-loggen visar att
publiceringen **lyckades (201) på 16 236 ms**. Handlern är billig (fem
frågor + `UPDATE matches SET stale`), varm publicering mätt tre gånger:
**32, 45, 62 ms**. Ingen jobbrad loggad i fönstret; pg-boss-schemana (`*/15`,
`0 */6`) faller inte på tidpunkten. Omkörning isolerad: **alla fem UI-kontroller
gröna**. Rotorsaken är inte fastställd — bevakas som M20; återkommer den är det
låskonkurrens mellan inbyggd worker och request i samma process (finns inte på
Vercel).

### F9 — Kontovägg före första matchningen är en odokumenterad premiss (BESLUT)

`/v1/funding-opportunities` och matchningen kräver inloggning (401); den
statiska ytan visar katalogen fritt. PRODUCT_DOCTRINE nämner inte konto, och
`tools/doctrine.mjs` kontrollerar det inte. Inte nödvändigtvis fel — men
konkurrensargumentet mot Grantigo är "hur lite användaren behöver göra", och
en registrering är ett formulär. Antingen dokumenteras kontot som medveten
gräns, eller släpps första matchningen anonymt. M19.

### F10 — Adversariella probes (GRÖNT)

CSP `default-src 'self'; script-src 'self'; frame-ancestors 'none'` m.m., HSTS,
nosniff, `Referrer-Policy: no-referrer`. Främmande origin får inget
`Access-Control-Allow-Origin`. Skyddade rutter 401; `/v1/internal/readiness`
404 utan auth (döljer existens). 30 MB POST → 413. Path traversal, SQL-mönster,
NUL och `<script>` i slug → 404. DELETE på läsrutt → 404. Registrerings-
rate-limit slog till (429) under probeserien — som den ska.

### F11 — Körda batterier (GRÖNT)

| Kontroll | Utfall |
|---|---|
| `npm run verify` | 24/24 (seed-integritet + motorsimulering tillagda) |
| core-tester | 100/100 |
| api-tester | 225/225 |
| `demo:build` + `demo:check` | 10/10 webbläsarkontroller |
| `verify:ui` (uicheck2, 8, 9, 12, 13) | 5/5 vid isolerad körning |
| `verify:sim30` | 30/30 användare, 0 anomalier, 0 utan resultat, 76 kr, 5 dokument |
| `gate:ux` | 94 sidor × 2 vyer, 0 fynd |
| `i18n:cov` | 1725/1725 förekomster (100 %) |
| `openapi` | i synk (regenerering gav ingen diff) |
| `npm audit --omit=dev` | 0 sårbarheter (4 måttliga i dev-kedjan, LIMITATIONS §11) |
| Belastningstest 30 s × 25 samtidiga | **324 req/s, 9 753 anrop, 0 fel.** Läsvägar p50 49 ms / p99 ≈ 105 ms; `POST …/matches` (ommatchning mot 85 stöd) p50 222 ms / p95 320 ms / **p99 2,4 s** under last — värt att bevaka men ingen 5xx. Första körningen "föll" med 58 785 × 429 av 59 083: all trafik kom från en IP och per-IP-gränsen (300/min) gjorde sitt jobb, med p99 25 ms för avvisningarna. Kapacitetstalet kräver `RATE_LIMIT_MAX` höjd; noterat i skriptets huvud. |

### F12 — Sandlådan: Postgres dog tre gånger under passet (MILJÖ)

Varje gång omstartad enligt CLAUDE.md regel 7. Den kända första-verify-
flakigheten efter omstart inträffade två gånger; andra körningen grön båda
gångerna. Ingen produktpåverkan.

### F13 — Källhämtningens räknare sa "0 fel" när 100 % var fel (RÄTTAT)

Cron-jobbet `source-fetch` svarade `{"fetched":37,"failed":0}`. I databasen:
**36 av 36 hämtade källor har `last_error = 'HTTP 403'`** (sandlådans proxy).
`runSourceFetchAll` räknade `failed` bara när `fetchSource` kastade —
HTTP-fel sparas som snapshot med `http_status` och `last_error` utan att
kasta, och räknades som `fetched`. En operatör som läser cron-utdata hade
trott att alla källor var friska. Rättat: jobbet rapporterar nu
`{fetched, httpErrors, failed}` där icke-2xx räknas separat.

**F13b:** jag skrev själv "37 av 37 källor, 0 fel" i `DEPLOY-AGENT.md` tidigare
i dag, på grund av samma räknare. Rättat med hänvisning hit. Lärdomen är
generell: ett jobbs sammanfattning är ett *påstående*, inte ett bevis —
verifiera mot lagrat tillstånd.

## Del 2 — motorsimulering i stor skala (samma dag, efter del 1)

På begäran: "kör en så omfattande simulering du kan, använd resultatet och
laga". `tools/simulate-engine.mjs` genererar ~11 000 koherenta personor ur
seedens eget faktarum (11 sökandetyper, två spår för privatpersoner exakt som
intagen bygger dem) och kör dem mot alla 85 stöd direkt i `packages/core` —
**952 000 matchningar på 1,4 sekunder**, deterministiskt, utan server. Ett
andra läge (`--intake`) känner bara till intagets fakta och mäter därmed
frågebördan som användaren möter den.

### F14 — Åldersgränsen hade ingen nedre gräns; en 17-åring passerade båda ungdomsstöden (RÄTTAT)

M15 beskrev att `person.ageUnder29` var överlastad (18–28 mot 19–29). Simu-
leringen visade att det var värre: härledningen `age <= 28` hade **ingen
nedre gräns alls**. Med produktens egen härledning:

| Ålder | Bostadsbidrag unga (18–28) | Aktivitetsersättning (19–29) |
|---|---|---|
| 17 | eligible ✗ | eligible ✗ |
| 18 | eligible ✓ | eligible ✗ |
| 29 | excluded ✓ | excluded ✗ |

Rättat i grunden: `deriveAgeFacts()` i `packages/core/src/facts.ts` är nu
**den enda** härledningen (kopian låg på fyra ställen — webb, demo,
simulate30, gendocs — med kommentaren "håll dem i synk!"), med två separata
fakta `person.age18to28` och `person.age19to29`. Seedens två kriterier pekar
om; frågetexterna är oförändrade så inga översättningar berörs. Webben fick
därmed sitt första körtidsberoende på core — CLAUDE.md:s "apps/web importerar
core för typer" var inte sant förrän nu. Sju nya core-tester på gränserna.
Efter rättningen: alla sex gränsfall ✓.

### F15 — En återkommande omgång som stängt uteslöt stödet för alla, för alltid (RÄTTAT i motorn)

`rf-lok-stod` (`recurring`, `closesAt 2026-08-25`) var DÖD: motorn uteslöt
hårt på passerat datum oavsett modell, och `deadlines.ts` rullar inte fram
återkommande omgångar. Ett LOK-stöd med två omgångar om året var alltså
osynligt från 26 augusti tills någon manuellt satte nästa datum — och ingen
vakt sa till (`seo-dataqa` vaktar bara det omvända: stängt som visas öppet).

Motorn tar nu `deadlineModel`: `recurring`/`upcoming_round` med passerat
datum är **inte** uteslutet utan `unknown` med förklaringen "senaste omgången
har stängt — nästa omgång är inte publicerad ännu". API:t, demon och simu-
latorn skickar modellen; gamla anropare utan modell får det gamla beteendet.
Fyra tester. `seed:integrity` fick klass H som flaggar exakt dessa stöd som
kureringsläge (M21).

### F16 — Frågebördan efter intaget: 20 frågor i median, 30 vid p90 (MÄTT, PRODUKTSIGNAL)

I intagsläget möter en privatperson på personspåret **p50 20, p90 30, max
37** unika öppna frågor. F-STABIL visar högst sex åt gången i informations-
värdesordning, så användaren går igenom ~20 svar för full upplösning.
Frågorna som avgör flest stöd (summerat över alla personor):
funktionsnedsättning/långvarig sjukdom i familjen, "planerar ni att anställa",
förfinansieringsförmåga, flytt utomlands. Det är §7-ordningens facit. M22.

### Vad simuleringen INTE hittade (och som därför är belagt)

- **Ingen sektorsläcka** (LÄCKA 0) över 4 000 personspårs-personor —
  F-RELEVANS håller i hela faktarummet, inte bara i audit-relevans tio personor.
- **Inget stöd är dött** i standardläget efter rättningarna (DÖD 0; LOK-stödet
  står som "väntar").
- Datumsvepet 12 månader framåt: inget stöd flippar oväntat.

Simuleringens egna första fynd — åtta "döda" stöd — visade sig vara
generatorns lucka: den byggde bara personspåret, och konstnärer når
kulturstöden via projektspåret (`pr-artist`). Generatorn speglar nu intagen.
Det är i sig en lärdom: en simulator som inte bygger personor exakt som
produkten gör producerar falsklarm som ser ut som produktfel.

`npm run sim:engine` · `npm run sim:engine:intake` · steg i verify (2 000
personor, `--strict` fäller på DÖD/LÄCKA).

## Vad som byggdes under revisionen

- `tools/seedintegrity.mjs` + `npm run seed:integrity` + steg i verify (klass
  C/G fäller, övriga rapporteras).
- `tools/genmanual.mjs`: reaktivt stödantal.
- `runSourceFetchAll`: ärlig räknare (`httpErrors`).
- PERFECTION_BACKLOG: M16 utökad; M17–M20 nya.

## Vad som INTE gick att testa härifrån

- Källornas verkliga nåbarhet (proxyn ger 403 på myndighetsdomäner) — mäts
  först efter deploy.
- Vercel-produktionen (API kraschar på saknad `DATABASE_URL`; operatörssteg).
- Riktig Stripe/Swish-betalning.
